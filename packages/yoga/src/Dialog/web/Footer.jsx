import React from 'react';

import Box from '../../Box';

const Footer = props => (
  <Box as="footer" d="flex" justifyContent="center" gap="large" {...props} />
);

Footer.displayName = 'Dialog.Footer';

export default Footer;
