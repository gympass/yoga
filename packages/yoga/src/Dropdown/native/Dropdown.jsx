import React from 'react';
import styled from 'styled-components';

const StyledDropdown = styled.Text``;

const Dropdown = props => <StyledDropdown {...props} />;

Dropdown.propTypes = {};

Dropdown.defaultProps = {};

export default Dropdown;
