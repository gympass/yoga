import { Link as GatsbyLink } from 'gatsby';

### Reference

Gympass `<Chips />` description

### Usage

```javascript type=expo state
const { useState } = React;

function App() {
  const [first, toggleFirst] = useState(false);
  const [second, toggleSecond] = useState(false);
  const onToggleFirst = () => toggleFirst(!first);
  const onToggleSecond = () => toggleSecond(!second);

  return (
    <ThemeProvider>
      <Chips selected={first} onPress={onToggleFirst}>
        Classes
      </Chips>
      <Chips selected={second} onToggle={onToggleSecond}>
        Gyms and studios
      </Chips>
    </ThemeProvider>
  );
}

export default App;
```

With icons:

```javascript type=expo state
const { useState } = React;

function App() {
  const [first, toggleFirst] = useState(true);
  const [second, toggleSecond] = useState(false);
  const [third, toggleThird] = useState(false);
  const onToggleFirst = () => toggleFirst(!first);
  const onToggleSecond = () => toggleSecond(!second);
  const onToggleThird = () => toggleThird(!third);

  return (
    <ThemeProvider>
      <Chips selected={first} onPress={onToggleFirst} icons={[, Time]}>
        Open now
      </Chips>
      <Chips selected={second} onToggle={onToggleSecond} icons={[ChevronDown]}>
        Activities
      </Chips>
      <Chips
        selected={third}
        onToggle={onToggleThird}
        icons={[ChevronDown, MapPin]}
      >
        Location
      </Chips>
    </ThemeProvider>
  );
}

export default App;
```

With system props:

```javascript type=expo state
const { useState } = React;

function App() {
  const [first, toggleFirst] = useState(true);
  const [second, toggleSecond] = useState(false);
  const onToggleFirst = () => toggleFirst(!first);
  const onToggleSecond = () => toggleSecond(!second);

  return (
    <ThemeProvider>
      <Chips
        selected={first}
        onToggle={onToggleFirst}
        elevation="small"
        width={150}
        >
        Open now
      </Chips>
      <Chips
        selected={second}
        onToggle={onToggleSecond}
        ml="medium"
        >
        Activities
      </Chips>
    </ThemeProvider>
  );
}

export default App;
```

### Props

<p>
  The Chips component also has support for {''}

  <GatsbyLink to="/system/backgroundColor">backgroundColor</GatsbyLink>, {''} 
  <GatsbyLink to="/system/border">border</GatsbyLink>, {''}
  <GatsbyLink to="/system/elevation">elevation</GatsbyLink>, {''}
  <GatsbyLink to="/system/layout">layout</GatsbyLink>, {''}
  <GatsbyLink to="/system/spacing">spacing</GatsbyLink>, 
  and <GatsbyLink to="/system/typography">typography</GatsbyLink> props 

</p>

<PropsTable component="Chips" platform="native" />
