---
title: 'Outline'
metaTitle: 'Button.Outline'
metaDescription: 'Button outline looks and behavior'
---

# Button.Outline

### Reference

#### Web only

Gympass `<Button />` component implements all our design guidelines such as default spacings, borders and color pallete tokens.
You can use all differente compounds like `<Button />` and `<Button.Text />` combining with their props such as `small` or `inverted` so the button become the desired variation.

### Usage

#### Default

```javascript
<Box display="flex" justifyContent="space-evenly" width="100%">
  <Button.Outline>Find an activity</Button.Outline>
  <Button.Outline secondary>Find an activity</Button.Outline>
</Box>
```

#### As an anchor

```javascript
<Box display="flex" justifyContent="space-evenly" width="100%">
  <Button.Outline href="http://www.gympass.com" target="blank">
    Find an activity (external link)
  </Button.Outline>
</Box>
```

#### With icon

```javascript
<Box display="flex" justifyContent="space-evenly" width="100%">
  <Button.Outline icon={Booking}>Find an activity</Button.Outline>
  <Button.Outline icon={Booking} secondary>
    Find an activity
  </Button.Outline>
</Box>
```

#### With Loading

```javascript state
render(() => {
  const [loading, setLoading] = useState(true);
  const onChange = () => setLoading(!loading);

  return (
    <Box display="flex" flexDirection="column" justifyContent="center" alignItems="start" width="auto" gap="large">
      <Box>
        <Text>Loading</Text>
        <Checkbox.Switch checked={loading} onChange={onChange} />
      </Box>

      <Box display="flex" justifyContent="center" alignItems="center" gap="xxlarge">
        <Box display="flex" flexDirection="column" justifyContent="center" alignItems="center" gap="xxlarge">
          <Button.Outline isLoading={loading}>Find an activity</Button.Outline>
          <Button.Outline secondary isLoading={loading}>Find an activity</Button.Outline>
        </Box>

        <Button.Outline href="http://www.gympass.com" target="blank" isLoading={loading}>
          Find an activity (external link)
        </Button.Outline>

        <Box display="flex" flexDirection="column" justifyContent="center" alignItems="center" gap="xxlarge">
          <Button.Outline icon={Booking} isLoading={loading}>Find an activity</Button.Outline>
          <Button.Outline icon={Booking} secondary isLoading={loading}>
            Find an activity
          </Button.Outline>
        </Box>
      </Box>
    </Box>
  );
});
```

### Props

<PropsTable component="Button" platform="web" />
