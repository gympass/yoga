---
title: 'Datepicker'
metaTitle: 'Datepicker'
metaDescription: 'Datepicker Component'
---

# Datepicker

### Reference

The `<Datepicker />` component is a date picker that allows the user to select a single date or a range of dates.

Both `startDate` and `endDate` are expected to be in the [`Date`](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Date) format.

### Usage

### Single date Datepicker

```javascript state
render(() => {
  const [selectedDate, setSelectedDate] = useState();

  return (
    <Datepicker
      type="single"
      startDate={selectedDate}
      onSelectSingle={setSelectedDate}
    />
  );
});
```

### Range date Datepicker

```javascript state
render(() => {
  const [startDate, setStartDate] = useState();
  const [endDate, setEndDate] = useState();

  const onSelectRange = (startDate, endDate) => {
    console.log({ startDate, endDate });
  };

  return (
    <Datepicker
      type="range"
      startDate={startDate}
      endDate={endDate}
      onSelectRange={onSelectRange}
    />
  );
});
```

### Range date Datepicker with custom onSelectRange

```javascript state
render(() => {
  const [startDate, setStartDate] = useState();
  const [endDate, setEndDate] = useState();

  const onCustomSelectRange = selectedDate => {
    console.log({ selectedDate });
    if (!startDate || (startDate > selectedDate && !endDate) || endDate) {
      setStartDate(selectedDate);
      if (endDate) setEndDate(undefined);
    } else {
      setEndDate(selectedDate);
    }
  };

  return (
    <Datepicker
      type="range"
      startDate={startDate}
      endDate={endDate}
      customOnSelectRange={onCustomSelectRange}
    />
  );
});
```

### Single Datepicker with default selected date

```javascript state
render(() => {
  const d = new Date();
  d.setDate(d.getDate() + 2);
  const [selectedDate, setSelectedDate] = useState(d);

  return (
    <Datepicker
      type="single"
      startDate={selectedDate}
      onSelectSingle={setSelectedDate}
    />
  );
});
```

### Range Datepicker with default start date and end date

```javascript state
render(() => {
  const start = new Date();
  const end = new Date();

  end.setDate(start.getDate() + 15);
  const [startDate, setStartDate] = useState(start);
  const [endDate, setEndDate] = useState(end);

  const onSelectRange = (startDate, endDate) => {
    console.log({ startDate, endDate });
  };

  return (
    <Datepicker
      type="range"
      startDate={startDate}
      endDate={endDate}
      onSelectRange={onSelectRange}
    />
  );
});
```

### Single Datepicker with custom placeholder

```javascript state
render(() => {
  const [selectedDate, setSelectedDate] = useState();

  return (
    <Datepicker
      type="single"
      startDate={selectedDate}
      onSelectSingle={setSelectedDate}
      placeholder="This is a custom placeholder :)"
    />
  );
});
```

### Datepicker with past dates disabled

```javascript state
render(() => {
  const [selectedDate, setSelectedDate] = useState();

  return (
    <Datepicker
      type="single"
      startDate={selectedDate}
      onSelectSingle={setSelectedDate}
      disablePastDates
    />
  );
});
```

### Datepicker with future dates disabled

```javascript state
render(() => {
  const [selectedDate, setSelectedDate] = useState();

  return (
    <Datepicker
      type="single"
      startDate={selectedDate}
      onSelectSingle={setSelectedDate}
      disableFutureDates
    />
  );
});
```

### Datepicker with past dates disabled from a specific date

```javascript state
render(() => {
  const [selectedDate, setSelectedDate] = useState();
  const d = new Date();
  d.setDate(d.getDate() + 4);

  return (
    <Datepicker
      type="single"
      startDate={selectedDate}
      onSelectSingle={setSelectedDate}
      disablePastFrom={d}
    />
  );
});
```

### Datepicker with future dates disabled from a specific date

```javascript state
render(() => {
  const [selectedDate, setSelectedDate] = useState();
  const d = new Date();
  d.setDate(d.getDate() - 4);

  return (
    <Datepicker
      type="single"
      startDate={selectedDate}
      onSelectSingle={setSelectedDate}
      disableFutureFrom={d}
    />
  );
});
```

### Datepicker disabled

```javascript state
render(() => {
  const [selectedDate, setSelectedDate] = useState(new Date());

  return (
    <Datepicker
      type="single"
      startDate={selectedDate}
      onSelectSingle={setSelectedDate}
      disabled
    />
  );
});
```

### Datepicker with error

```javascript state
render(() => {
  const [selectedDate, setSelectedDate] = useState();

  return (
    <Datepicker
      type="single"
      startDate={selectedDate}
      onSelectSingle={setSelectedDate}
      error="This is an error!"
    />
  );
});
```

### Datepicker custom onOpen action

```javascript state
render(() => {
  const [selectedDate, setSelectedDate] = useState();
  const onOpen = isOpen => {
    alert(`onOpen custom action. isOpen -> ${isOpen}`);
  };

  return (
    <Datepicker
      type="single"
      startDate={selectedDate}
      onSelectSingle={setSelectedDate}
      onOpen={onOpen}
    />
  );
});
```

### Props

<PropsTable component="Datepicker" platform="web" />
