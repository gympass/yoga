---
title: 'Fonts'
metaTitle: 'Theme Fonts'
metaDescription: 'How fonts are used inside our theme'
order: 10
---

## Fonts

Fonts can define our brand and the way we express ourselves through titles and
texts.

### Usage

This is a simple usage:

```javascript type=highlight
import styled from 'styled-components';
import { theme } from '@gympass/yoga';

const MyCoolComponent = styled.div`
  font: ${theme.fonts.rubik};
`;
```

<ThemeFonts />
