---
title: 'Font-weights'
metaTitle: 'Theme Font-Weights'
metaDescription: 'How font weights are used inside our theme'
order: 8
---

## Font-Weights

Font Weights define the weight of a typography to express relevance.

### Usage

This is a simple usage:

```javascript type=highlight
import styled from 'styled-components';
import { theme } from '@gympass/yoga';

const MyCoolComponent = styled.div`
  font-weight: ${theme.fontWeights.medium};
`;
```

<ThemeFontWeights />
