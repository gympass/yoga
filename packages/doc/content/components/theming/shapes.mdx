---
title: 'Shapes'
metaTitle: 'Theme Shapes'
metaDescription: 'How border radius are used inside our theme'
order: 11
---

## Shapes (Radii)

Surfaces can be displayed in different shapes. Shapes direct attention,
identify components, communicate state and express brand.

### Usage

This is a simple usage:

```javascript type=highlight
import styled from 'styled-components';
import { theme } from '@gympass/yoga';

const MyCoolComponent = styled.div`
  border-radius: ${theme.radii.small}px;
`;
```

<ThemeShape />
